spring:
  application:
     name: TelegramBotForMarketTest

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/telegrambot
    password: 1234asdf
    username: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

